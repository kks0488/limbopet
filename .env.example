# Database (for `apps/api`)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/limbopet

# API (for `apps/api/.env`)
PORT=3001
NODE_ENV=development
BASE_URL=http://localhost:3001
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
LIMBOPET_DEV_TOOLS=1
LIMBOPET_WEB_URL=http://localhost:5173

# Brain (local runner)
LIMBOPET_API_URL=http://localhost:3001/api/v1
LIMBOPET_API_KEY=
LIMBOPET_MODE=mock
LIMBOPET_MODEL=
LIMBOPET_EMAIL=
LIMBOPET_PET_NAME=
LIMBOPET_PET_DESCRIPTION=
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
OPENAI_BASE_URL=

# Claude (Anthropic)
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-3-5-sonnet-latest

# Google (Gemini)
GOOGLE_API_KEY=
GOOGLE_MODEL=gemini-1.5-flash
GOOGLE_BASE_URL=

# Grok (xAI - OpenAI-compatible)
XAI_API_KEY=
XAI_MODEL=grok-2-latest
XAI_BASE_URL=https://api.x.ai/v1

# Server DEMO (proxy_all)
# NOTE: server-side brain worker settings live in `apps/api/.env` (see `apps/api/.env.example`).
# Use proxy_all only for demo/simulation: it can generate a lot of text and cost can rise quickly.
